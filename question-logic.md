## HTTP -> question controller -> 

## controller
api 란? https://www.oracle.com/kr/cloud/cloud-native/api-management/what-is-api/

### @RequestMapping("") 이유
1. @RequestMapping은 HTTP Request로 들어오는 url을 연결 시켜줌. 
2. `@RequestMapping("/api/questions")`는 질문 관련 일은 전부 여기서 처리하겠다는 의미
3. 중간에 바뀌면 안되니까 final로 설정함

### 
